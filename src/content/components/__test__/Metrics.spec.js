describe('>>> d3', () => {

    it('+++ for containers add these tests to test dispatch https://hackernoon.com/unit-testing-redux-connected-components-692fa3c4441c', () => {});

    it('+++ Snapshot of d3 components', () => {});

    it('+++ add test to make sure data is sorted', () => {});

    it(`+++ make sure tooltip on graph shows on left when on left side of graph and when it's too low`, () => {});
});

describe('>>> d3 - redux integration to-do', () => {
    it('++ fix key prop by adding unique value to redux with crc32 hasing', () => {})
    
    it('++ substitute hard coded object key with period option from redux store', () => {})
    
    it('++ fix up slice with time interval from redux store', () => {})
    
    it('++ add scatter plot as an option via props/redux to the graph (like enable prediction)', () => {})

    it('++ review using mean as indicator for changed data, use Redux store marker', () => {})
});